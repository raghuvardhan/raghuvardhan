class experience{
    constructor(company, title, project, startDate, toDate, profile)
    {
        this.company = company;
        this.startDate = startDate;
        this.toDate = toDate;
        this.title = title;
        this.project = project;
        this.profile = profile;
    }
}

export {experience}